server:
  port: 8080
spring:
  application:
    name: "votamas"
  devtools:
    add-properties: false
  r2dbc:
    url: r2dbc:postgresql://localhost:5433/votamas?currentSchema=vota_mas
    username: postgres
    password: 12345
  data:
    r2dbc:
      repositories:
        enabled: true
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
  datasource:
    url: "jdbc:h2:mem:test"
    username: "sa"
    password: ""
    driverClassName: "org.h2.Driver"
  jpa:
    databasePlatform: "org.hibernate.dialect.H2Dialect"
r2dbc:
  url: r2dbc:postgresql://localhost:5433/votamas?currentSchema=vota_mas
  username: postgres
  password: 12345
  data:
    r2dbc:
      repositories:
        enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
adapters:
  r2dbc:
    host: "localhost"
    port: 5433
    database: "votamas"
    schema: "vota_mas"
    username: "postgres"
    password: 12345
jwt:
  secret: hh_clave_super_william_de_reppppp_32_232DFE
  expiration: 86400000 # 1 day in milliseconds
entries:
  reactive-web:
    base-path-api: "/api/v1"