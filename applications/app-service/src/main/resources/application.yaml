server:
  port: 8080
spring:
  application:
    name: "votamas"
  devtools:
    add-properties: false
  profiles:
    include: null
  jpa:
    databasePlatform: "org.hibernate.dialect.H2Dialect"
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
adapters:
  r2dbc:
    host: "localhost"
    port: 5432
    database: "votamas"
    schema: "vota_mas"
    username: "postgres"
    password: 12345
jwt:
  secret: hh_clave_super_william_de_reppppp_32_232DFE
  expiration: 86400000 # 1 day in milliseconds
  issuer: http://localhost:8080
entries:
  reactive-web:
    base-path-api: "/api/v1"
